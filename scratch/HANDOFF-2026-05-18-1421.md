# Handoff — 2026-05-18 14:21 MDT

Generated to bridge into a fresh Claude Code session. Session resume doesn't restore in-process tentacles (documented Claude Code limitation), so we use handoff docs instead.

Last session: focused on the v0.6 Render self-host arc + a process reflection. Hub PR #262 shipped + folded; notes#139 shipped; architecture pivot to option A locked in.

---

## TL;DR

1. **Hub PR #262 is at rc.4, ready for Aaron's merge click.** This is the Phase 1A foundation of v0.6 — admin module mgmt API + supervisor + container-mode runtime install. Reviewer LGTM with one real nit (security scope elevation) already folded in rc.4.
2. **Architecture decision: option A locked in** — v0.6 self-host = single Render container running hub-as-supervisor. Modules installed at runtime via admin SPA, persisted on `/parachute/` disk. v0.6 ships only when a friend can fork + click Deploy + install vault/notes/scribe via web UI in <5min, all working.
3. **Next dispatch when ready**: hub#259 (first-boot wizard) + hub#260 Phase 1B (real-time status + per-module logs) + parachute.computer#42 (/deploy page) + patterns#69 (container-mode pattern doc).

---

## In-flight work

### parachute-hub #262 — Phase 1A admin module mgmt (0.5.10-rc.4)

- **State**: open, reviewer LGTM (5 nits, the 1 real one is folded in rc.4)
- **HEAD**: `90abfd0` on `ag-unforced-dev` (11 commits past main)
- **Gates last seen**: 1347 pass / 1 known flake / 30555 expects across 76 files (hub); 115/0 SPA. Tests on the rc.4 fold pass (15/0 for api-modules-ops alone)
- **Next action**: **Aaron's merge click**. Then `npm publish --tag rc` to ship 0.5.10-rc.4 to `@rc`.
- **Context**: This is the v0.6 release-bar PR. Container smoke partially verified (docker build ✓, docker run ✓, /health ✓, auth gate ✓). NOT verified: browser walk (login → /admin/modules → click Install vault → observe pid). CSRF on /login blocks curl; needs a real browser. Recommend a manual smoke pass before merge OR right after on Render.
- **Reviewer nits NOT folded (filed as follow-ups)**:
  - hub#263 — `supervisor.stop()` should await child proc.exited (Phase 1B polish)
  - hub#264 — SIGKILL fallback after SIGTERM timeout (Phase 1B polish)
  - hub#265 — upgrade-on-unsupervised test gap (small test follow-up)

### parachute-vault #199 — cloud-shape research (do-not-merge)

- **State**: open, pre-existing from before this session
- **Branch**: `docs/cloud-shape-research`
- **Next action**: review when bandwidth allows; not v0.6-blocking. Likely closeable since option A was chosen.

### parachute.computer #26 — interest list v1

- **State**: open, pre-existing
- **Branch**: `feat/interest-list-v1`
- **Next action**: not this session's scope; review when ready.

### Open issues filed this session (no PR yet)

**Hub:**
- **hub#257** (meta-tracker) — v0.6 Render self-host arc; **body reshaped for option A**
- **hub#259** — first-boot web wizard at /admin/setup (Phase 1 critical-path)
- **hub#260** — admin SPA module management (now Phase 1 critical-path; **body has BUN_INSTALL design notes**)
- **hub#263 / #264 / #265** — three Phase 1B follow-ups from #262 reviewer

**Vault:**
- **vault#341** — deprecate standalone render.yaml as primary path (low priority)

**Patterns:**
- **patterns#67** — service-to-service-auth wording update for operator bearer
- **patterns#68** — format-aware notes + surfaces pattern doc
- **patterns#69** — container-mode + hub-as-supervisor pattern doc (from #262)

**Site:**
- **parachute.computer#42** — /deploy page (one Deploy button, hub-only target)

---

## Pending Aaron's hand (off-Claude)

1. **Merge hub#262** when ready (after final smoke)
2. **`npm publish --tag rc` for hub** (0.5.10-rc.4) — note: prior session's publish to `@rc` of 0.5.10-rc.2 may not have completed; `npm view @openparachute/hub@rc version` was still showing `0.5.9-rc.6` when last checked. Verify before assuming.
3. **`npm publish --tag rc` for notes** (0.3.15-rc.13)
4. **`npm publish` for scribe 0.4.2 stable** (still pending from earlier arc; not v0.6-blocking)
5. **Beta email review** at `/Users/parachute/ParachuteComputer/BETA-EMAIL-since-launch.md`
6. **Blog outline review** at `/Users/parachute/ParachuteComputer/BLOG-OUTLINE-export-as-projection.md`
7. **Branch protection check** on parachute.computer (some prior PRs went direct-to-main)

---

## Decisions made this session

1. **Option A locked in for v0.6 deploy shape**: single Render container running hub-as-supervisor, modules installed at runtime via admin SPA, persisted on `/parachute/`. Rejected multi-container ($7 × N per Render Starter doesn't fit modular orientation). Captured in memory at `project_v06_single_container_hub_as_supervisor.md`. Also in scratch THINKING-multi-user-and-cloud.md (with the architecture-pivot section appended).
2. **v0.6 release bar**: no partial ship. v0.6 ships only when friend-experience-loop is complete (deploy + install vault/notes/scribe via web, <5min, all working).
3. **hub#260 promoted Phase 2 → Phase 1 critical-path**: without it, deployed hub is stuck empty.
4. **Handoff docs replace session resume**: in-process tentacles don't survive `/resume` (documented Anthropic limitation). Use `/handoff` slash command (newly added at `~/ParachuteComputer/.claude/commands/handoff.md`) to bridge sessions cleanly.
5. **Notes Phase 2 format-aware rendering shipped** (PR #139 merged at f2ddc77, 0.3.15-rc.13).
6. **Patterns follow-ups filed instead of fold-in-PR** for cross-cutting documentation that doesn't fit the PR diff (patterns#67-69).

---

## Open questions / pending decisions

1. **Dispatch model going forward** — Option 1 (subagent-default) vs Option 2 (keep tentacles, tighten hygiene). Discussed but not formally decided. The handoff-doc approach (this thing) sidesteps the session-resume issue for either. My lean: subagent-default for substantial single-task work; tentacles only for genuine iterative back-and-forth.

2. **Memory consolidation** — 36 entries today, identified consolidation candidates (-8 entries via merging tentacle hygiene, PR cadence, RC versioning, act-don't-ask clusters). Not executed in this session. Worth doing in the next session.

3. **Workspace CLAUDE.md "Tentacles" section** — drafted ideas for tightening but didn't apply. CLAUDE.md is higher-stakes than memory; propose-then-confirm.

4. **Design doc for option A** — proposed location `parachute.computer/design/2026-05-18-v06-deploy-architecture.md`. Not written this session. Worth doing.

5. **Branch protection on parachute.computer** — needs verification; prior direct-to-main pushes happened during launch arc.

---

## Local working-tree state per repo

All branches synced to origin unless noted:

- **parachute-hub**: `ag-unforced-dev` at `90abfd0`. Clean. 11 commits past main (PR #262).
- **parachute-vault**: `ag-unforced-dev` at `f1db2f5` (= main, post-vault#340 merge). Clean. Untracked `scratch/` (this session's HANDOFF + reflection docs).
- **parachute-notes**: `ag-unforced-dev` at `aa240a7`. Has notes#139 commits past main (already merged via PR f2ddc77 squash). Reset to main on session start.
- **parachute-scribe**: `ag-unforced-dev` at `b787167` past main `78c7cee`. Pre-existing state; not touched this session.
- **parachute-agent**: `main` at `ffa70ba`. **Has uncommitted change** in `src/mcp/tools/channels.test.ts` (12 lines, predates this session, last commit on that file 13 days ago). Not related to current arc — Aaron may want to investigate or revert.
- **parachute-patterns**: `ag-unforced-dev` at `e93947f` (= main). Clean. Untracked: `_site/`, `research/cloudflare-hosted-vault.md`, `research/knowledge-tool-data-models.md`, `research/tana-deep-dive.md` (all pre-existing exploratory research, not touched this session).
- **parachute.computer**: `main` at `b8ff584`. Clean.

---

## Tentacle state (will not survive session end)

In-process tentacles die when the session ends. None had load-bearing context that wasn't captured in commits + this doc.

- **hub tentacle**: idle, last did PR #262 commit chain. Slot-squat caused multiple message duplications throughout the session — known Claude Code limitation. Next session, dispatch fresh as needed.
- All other tentacles: not dispatched this session.

---

## Pointers

### Scratch docs in this workspace
- `scratch/HANDOFF-2026-05-18-1421.md` — this doc
- `scratch/PROCESS-REFLECTION-2026-05-18.md` — observations on dispatch friction + cleanup recommendations (5 broken patterns, 3 dispatch-model options, 7 specific recommendations for next session)
- `scratch/THINKING-multi-user-and-cloud.md` — v0.6 strategy thinking, includes "Architecture pivot" section locking in option A
- `scratch/PROCESS-AUDIT-2026-05-17.md` — yesterday's patterns + open issues audit

### Newly-written memory entries (this session)
- `project_v06_single_container_hub_as_supervisor.md` — option A architectural decision

### Newly-updated memory entries
- `feedback_route_new_work_through_persistent_tentacle.md` — added `isActive: null is not a liveness signal` lesson

### Memory consolidation not yet done (proposed for next session)
- Merge 5 tentacle-hygiene entries into one comprehensive entry
- Merge `feedback_keep_moving` + `feedback_dont_block_on_silent_input` (overlap)
- Merge 3 PR-cadence entries
- Merge 2 RC-versioning entries
- Add new entry: "verify agent outputs against ground truth before acting" (lesson from today's reviewer false-positive — I cancelled a legit security fold based on un-verified grep that read working tree instead of committed state; restored later)

### New slash command
- `~/ParachuteComputer/.claude/commands/handoff.md` — the `/handoff` skill that generated this doc

### Recent research worth referencing
- Claude Code team-agents lifecycle research (in session context, not saved to disk) — confirmed Aaron's "borked on session restart" hypothesis is exactly right per Anthropic docs. Team-config is runtime state, `isActive` unreliable, in-process teammates don't survive resume. The documented recommendation is "spawn new teammates after resume." Handoff-doc bridges this cleanly.

---

## Next-session orientation checklist

1. Read `~/ParachuteComputer/CLAUDE.md` (workspace conventions — committed-core repos, governance, tentacle conventions)
2. Read `~/.claude/projects/-Users-parachute-ParachuteComputer/memory/MEMORY.md` (index)
3. **Read this handoff doc fully** before any action
4. Check `Current/Parachute` MCP for live ecosystem state if available
5. **Don't preemptively spawn tentacles** — dispatch only when work actually needs them
6. If a tentacle's session-prior work is referenced, the tentacle is gone — re-dispatch as needed
7. **Confirm hub#262 status** before doing other v0.6 work — has Aaron merged it? Has 0.5.10-rc.4 been published to `@rc`?

## High-leverage things to do early in next session

1. **Verify the npm publish state for hub** (`npm view @openparachute/hub@rc version`) — last session's publish may have hit a snag
2. **If hub#262 is merged**: dispatch hub tentacle on hub#259 (first-boot wizard) + hub#260 Phase 1B (real-time status). These two stack on top of the merged work.
3. **If hub#262 is not merged**: nudge Aaron after he's done with smoke pass; don't dispatch new hub work until #262 is on main.
4. **Memory consolidation** (~30 min) — the cleanup the user asked for; not blocking but worth doing while context is fresh.
5. **Design doc for option A** (~20 min) — save the v0.6 architecture decision to `parachute.computer/design/`.

---

## Notes for the team-lead in the next session

- **The handoff-doc pattern is new.** First use of it. If it works well, this is the canonical session-bridging mechanism going forward (not `/resume`).
- **Trust-but-verify on agent outputs.** This session caught a reviewer false-positive at the last minute (the security fold I "cancelled" was actually legit — reviewer was reading the committed file, I was reading the working tree). Spot-check citations against git-committed state before acting on agent findings.
- **The slot-squat / dual-actor phantom narrative is documented Claude Code behavior, not weird Parachute-specific thing.** Don't dwell on it. Trust the git log over the message channel.
